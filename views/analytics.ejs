<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>URL Analytics</title>
  <style>
    :root {
      --primary: #2c7be5;
      --primary-light: #e6f0ff;
      --text-dark: #212529;
      --text-muted: #6c757d;
      --bg: #f9fafb;
      --white: #ffffff;
      --shadow: rgba(0, 0, 0, 0.1);
      --success: #3ac47d;
    }
    * { box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: var(--bg);
      margin: 0;
      padding: 20px;
      color: var(--text-dark);
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    header {
      width: 100%;
      max-width: 900px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 30px;
      background: var(--white);
      padding: 15px 20px;
      box-shadow: 0 2px 8px var(--shadow);
      border-radius: 8px;
    }
    header h1 {
      margin: 0;
      font-weight: 700;
      font-size: 1.8rem;
      color: var(--primary);
    }
    header a.back-btn {
      text-decoration: none;
      color: var(--primary);
      font-weight: 600;
      border: 2px solid var(--primary);
      padding: 6px 14px;
      border-radius: 6px;
      transition: all 0.3s ease;
    }
    header a.back-btn:hover {
      background-color: var(--primary);
      color: var(--white);
    }
    .card {
      background: var(--white);
      max-width: 900px;
      width: 100%;
      border-radius: 10px;
      box-shadow: 0 4px 20px var(--shadow);
      padding: 30px 40px;
      margin-bottom: 30px;
    }
    .url-info {
      font-size: 1.2rem;
      word-break: break-all;
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 15px;
    }
    .url-info a {
      color: var(--primary);
      font-weight: 600;
      text-decoration: none;
      border-bottom: 2px solid var(--primary);
    }
    .url-info a:hover {
      color: #1a52b8;
      border-bottom-color: #1a52b8;
    }
    .qr-btn {
      background-color: var(--primary);
      color: var(--white);
      border: none;
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    .qr-btn:hover {
      background-color: #1a52b8;
    }
    .stats {
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--success);
      margin-bottom: 25px;
      display: inline-block;
      background-color: #daf7e4;
      padding: 8px 20px;
      border-radius: 30px;
    }
    table {
      width: 100%;
      border-spacing: 0 8px;
      font-size: 15px;
      box-shadow: 0 2px 8px var(--shadow);
      border-radius: 10px;
      overflow: hidden;
    }
    thead tr { background-color: var(--primary); color: white; }
    thead th { padding: 14px 20px; text-align: left; }
    tbody tr {
      background: var(--primary-light);
      transition: background-color 0.25s ease;
    }
    tbody tr:hover { background: #c3dbfc; }
    tbody td { padding: 12px 20px; }
    tbody td:first-child { font-weight: 700; }
    .no-data { text-align: center; color: var(--text-muted); padding: 40px 0; }

    /* Modal Styles */
    .modal {
      display: none;
      position: fixed;
      z-index: 999;
      left: 0; top: 0;
      width: 100%; height: 100%;
      background: rgba(0, 0, 0, 0.6);
      justify-content: center;
      align-items: center;
    }
    .modal-content {
      background: white;
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      max-width: 350px;
      width: 90%;
      position: relative;
    }
    .modal-content img {
      max-width: 200px;
      background: white;
      padding: 10px;
      border-radius: 12px;
    }
    .close-btn {
      position: absolute;
      right: 10px; top: 10px;
      background: transparent;
      border: none;
      font-size: 1.5rem;
      cursor: pointer;
      color: #666;
    }
    .close-btn:hover { color: black; }
  </style>
</head>
<body>

<header>
  <h1>URL Analytics</h1>
  <a href="/" class="back-btn">← Back</a>
</header>

<section class="card">
  <div class="url-info">
    <span>
      Redirect URL: 
      <a href="<%= redirectUrl %>" target="_blank" rel="noopener noreferrer"><%= redirectUrl %></a>
    </span>
    <button class="qr-btn" onclick="showQR('<%= shortId %>')">📷 Generate QR Code</button>
  </div>

  <div class="stats">Total Clicks: <%= totalClics %></div>

  <% if (analytics && analytics.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>#</th>
          <th>Timestamp (ms)</th>
          <th>Date & Time</th>
        </tr>
      </thead>
      <tbody>
        <% analytics.forEach((item, index) => { 
            const date = new Date(item.timestamp);
        %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= item.timestamp %></td>
            <td><%= date.toLocaleString('en-US', { dateStyle: 'medium', timeStyle: 'short' }) %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <p class="no-data">No analytics data available for this URL.</p>
  <% } %>
</section>

<!-- Modal -->
<div id="qrModal" class="modal">
  <div class="modal-content">
    <button class="close-btn" onclick="closeQR()">&times;</button>
    <h2>QR Code</h2>
    <img id="qrImage" src="<%=qrCodeData%> " alt="QR Code" />
    <p style="margin-top:10px;word-break:break-all;">
      <a id="qrLink" href="#" target="_blank" style="color: var(--primary);font-weight:600;"></a>
    </p>
  </div>
</div>

<script>
  function showQR(shortId) {
    const modal = document.getElementById('qrModal');
    const qrImg = document.getElementById('qrImage');
    const qrLink = document.getElementById('qrLink');
    const fullUrl = `http://localhost:8001/url/${shortId}`;
    
    // qrImg.src = fullUrl; // Server should directly serve QR image
    qrLink.href = fullUrl;
    qrLink.textContent = fullUrl ;

    modal.style.display = 'flex';
  }

  function closeQR() {
    document.getElementById('qrModal').style.display = 'none';
  }

  // Close when clicking outside modal
  window.onclick = function(event) {
    const modal = document.getElementById('qrModal');
    if (event.target === modal) {
      modal.style.display = "none";
    }
  }
</script>

</body>
</html>
